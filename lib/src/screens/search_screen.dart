import 'package:flutter/material.dart';

class SearBar extends StatelessWidget {
  const SearBar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("search here "));
  }
}
