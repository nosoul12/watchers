import 'package:flutter/material.dart';

class Fav_Screen extends StatelessWidget {
  const Fav_Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Your fav movies will be here'),
    );
  }
}
